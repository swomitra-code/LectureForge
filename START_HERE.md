# START HERE

1. Create a local folder such as `Documents\LectureForge`.
2. Copy this starter package into that folder.
3. Create folders: `source`, `media`, `config`, `work`, `output`, `src`, `tests`.
4. Put the cleaned Module 1 PPTX in `source/`.
5. Put the approved Slide 10 SKM-BLUE-7 WebM in `media/`.
6. Open PowerShell in the LectureForge folder.
7. Run `powershell -ExecutionPolicy Bypass -File .\check_prereqs.ps1`.
8. Run `git init`.
9. Run `codex`. If asked, choose **Sign in with ChatGPT**.
10. Paste `FIRST_CODEX_PROMPT.txt` into Codex.

Do not expand beyond Slide 10 until Codex has produced the MP4 and PPTX, closed and reopened the PPTX in desktop PowerPoint, and verified the video is embedded and plays.
